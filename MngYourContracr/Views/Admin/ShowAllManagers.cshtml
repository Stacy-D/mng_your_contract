@model IEnumerable<MngYourContracr.MngYourContractDatabase.Manager>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ShowAllManagers</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ManagerId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ManagerId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ManagerId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
